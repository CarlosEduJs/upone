# upone detects the .NET EF Core provider via the `App.csproj` referencing
# `Microsoft.EntityFrameworkCore` and runs `dotnet ef database update` to apply
# pending migrations after restore.
DATABASE_URL=sqlite:///./app.db