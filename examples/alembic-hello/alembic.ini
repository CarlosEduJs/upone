# Alembic example. upone detects `alembic.ini` and runs
# `<venv>/bin/python -m alembic upgrade head` after deps are installed.

[alembic]
script_location = alembic
sqlalchemy.url = sqlite:///./app.db